import { z } from "zod";
const actionSchema = z.object({ label: z.string().min(1), href: z.string().min(1).optional(), linkKey: z.string().min(1).optional(), intent: z.enum(["navigate", "contact", "call", "directions", "book", "buy", "subscribe", "download", "share", "submit"]).optional(), priority: z.enum(["primary", "secondary"]).optional(), variant: z.enum(["primary", "outline", "arrow", "cta"]).optional() }).refine((action) => Boolean(action.href || action.linkKey), { message: "Action requires href or linkKey" });
const formFieldSchema = z.object({ name: z.string().min(1), label: z.string().min(1), type: z.enum(["text", "email", "tel", "textarea", "select", "checkbox", "date", "time", "number"]), required: z.boolean().optional(), placeholder: z.string().optional(), min: z.union([z.number(), z.string()]).optional(), max: z.union([z.number(), z.string()]).optional(), step: z.union([z.number(), z.string()]).optional(), options: z.array(z.object({ label: z.string(), value: z.string() })).optional() });
const formSchema = z.object({ id: z.string().min(1), name: z.string().min(1), submitLabel: z.string().optional(), fields: z.array(formFieldSchema) });
const stringOrStringArraySchema = z.union([z.string(), z.array(z.string())]);
const contentItemSchema: z.ZodTypeAny = z.object({ id: z.string().optional(), eyebrow: stringOrStringArraySchema.optional(), title: z.string().optional(), subtitle: stringOrStringArraySchema.optional(), text: stringOrStringArraySchema.optional(), media: z.union([z.string(), z.array(z.string())]).optional(), links: z.array(actionSchema).optional(), meta: z.array(z.object({ label: z.string(), value: z.string().optional() })).optional(), tags: z.array(z.string()).optional(), category: z.string().optional(), group: z.string().optional(), featured: z.boolean().optional(), enabled: z.boolean().optional(), order: z.number().optional(), href: z.string().min(1).optional() });
const sourceSchema = z.object({ collection: z.string().min(1), query: z.object({ featured: z.boolean().optional(), category: z.string().optional(), group: z.string().optional(), enabled: z.boolean().optional(), limit: z.number().int().positive().optional() }).optional() });
export const sectionSchema = z.object({ id: z.string().min(1), type: z.literal("Section"), variant: z.enum(["classic", "editorial", "organic"]).optional(), tone: z.enum(["default", "inverse", "accent"]).optional(), surface: z.enum(["solid", "glass", "transparent"]).optional(), color: z.enum(["primary", "secondary", "special", "accent"]).optional(), layout: z.enum(["text", "statement", "split", "grid", "list", "gallery", "carousel", "media", "media-overlay", "timeline", "horizontal-scroll", "content-switcher"]).optional(), source: sourceSchema.optional(), content: z.object({ header: contentItemSchema.optional(), items: z.array(contentItemSchema).optional(), media: z.union([z.string(), z.array(z.string())]).optional(), links: z.array(actionSchema).optional(), form: z.union([z.string().min(1), formSchema]).optional() }).optional(), frame: z.boolean().optional(), itemAppearance: z.object({ frame: z.boolean().optional(), effect: z.enum(["none", "glass", "grain"]).optional() }).optional(), motion: z.enum(["none", "reveal", "scene"]).optional(), className: z.string().optional() });
export const blockRegistrySchema = z.record(z.string(), sectionSchema);
export const collectionsSchema = z.record(z.string(), z.array(contentItemSchema));
